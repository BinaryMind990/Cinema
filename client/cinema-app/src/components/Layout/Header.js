import { Fragment } from 'react';
import Navigation from './Navigation';

const Header = (props) => {
	return (
		<Fragment>
			<header>
				<Navigation />
			</header>
		</Fragment>
	);
};
export default Header;
